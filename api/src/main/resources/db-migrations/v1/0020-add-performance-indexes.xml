<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- Bookmarks indexes -->
    <changeSet id="20-1" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_bookmarks_user_id ON bookmarks(user_id)</sql>
    </changeSet>

    <changeSet id="20-2" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_bookmarks_other_user_id ON bookmarks(other_user_id)</sql>
    </changeSet>

    <changeSet id="20-3" author="bioritmic">
        <sql>CREATE UNIQUE INDEX IF NOT EXISTS idx_bookmarks_user_other_unique ON bookmarks(user_id, other_user_id)</sql>
    </changeSet>

    <changeSet id="20-4" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_bookmarks_user_timestamp ON bookmarks(user_id, timestamp DESC)</sql>
    </changeSet>

    <!-- Mailbox indexes -->
    <changeSet id="20-5" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_mailbox_from_user_id ON mailbox(from_user_id)</sql>
    </changeSet>

    <changeSet id="20-6" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_mailbox_to_user_id ON mailbox(to_user_id)</sql>
    </changeSet>

    <changeSet id="20-7" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_mailbox_conversation ON mailbox(from_user_id, to_user_id, timestamp)</sql>
    </changeSet>

    <changeSet id="20-8" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_mailbox_timestamp ON mailbox(timestamp DESC)</sql>
    </changeSet>

    <!-- GIS data indexes -->
    <changeSet id="20-9" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_gis_data_user_id ON gis_data(user_id)</sql>
    </changeSet>

    <changeSet id="20-9b" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_gis_data_lat_lon ON gis_data(lat, lon)</sql>
    </changeSet>

    <!-- Story views indexes -->
    <changeSet id="20-10" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_story_views_story_id ON story_views(story_id)</sql>
    </changeSet>

    <changeSet id="20-11" author="bioritmic">
        <sql>CREATE UNIQUE INDEX IF NOT EXISTS idx_story_views_story_viewer ON story_views(story_id, viewer_id)</sql>
    </changeSet>

    <!-- Subscriptions indexes -->
    <changeSet id="20-12" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_subscriptions_user_id ON subscriptions(user_id)</sql>
    </changeSet>

    <changeSet id="20-13" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_subscriptions_user_status ON subscriptions(user_id, status)</sql>
    </changeSet>

    <!-- Profile boosts indexes -->
    <changeSet id="20-14" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_profile_boosts_user_id ON profile_boosts(user_id)</sql>
    </changeSet>

    <!-- User blocks indexes -->
    <changeSet id="20-15" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_user_blocks_user_id ON user_blocks(user_id)</sql>
    </changeSet>

    <changeSet id="20-16" author="bioritmic">
        <sql>CREATE UNIQUE INDEX IF NOT EXISTS idx_user_blocks_user_other ON user_blocks(user_id, other_user_id)</sql>
    </changeSet>

    <!-- User photos indexes -->
    <changeSet id="20-17" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_user_photos_user_id_order ON user_photos(user_id, photo_order)</sql>
    </changeSet>

    <!-- Stories indexes -->
    <changeSet id="20-18" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_stories_expires_at ON stories(expires_at)</sql>
    </changeSet>

    <changeSet id="20-19" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_stories_user_id ON stories(user_id)</sql>
    </changeSet>

    <!-- Users indexes -->
    <changeSet id="20-20" author="bioritmic">
        <sql>CREATE UNIQUE INDEX IF NOT EXISTS idx_users_email_unique ON users(email)</sql>
    </changeSet>

    <changeSet id="20-21" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_users_gender_birthday ON users(gender, birthday)</sql>
    </changeSet>

    <!-- Meetings indexes -->
    <changeSet id="20-22" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_meetings_user_id ON meetings(user_id)</sql>
    </changeSet>

    <!-- Super likes indexes -->
    <changeSet id="20-23" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_super_likes_user_id ON super_likes(user_id)</sql>
    </changeSet>

    <changeSet id="20-24" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_super_likes_other_user_id ON super_likes(other_user_id)</sql>
    </changeSet>

    <!-- User push tokens indexes -->
    <changeSet id="20-25" author="bioritmic">
        <sql>CREATE INDEX IF NOT EXISTS idx_user_push_tokens_user_id ON user_push_tokens(user_id)</sql>
    </changeSet>

</databaseChangeLog>
